{
  "type": "module",
  "private": true,
  "scripts": {
    "clean": "rimraf src/.observablehq/cache",
    "build": "observable build",
    "dev": "observable preview",
    "deploy": "observable deploy",
    "observable": "observable",
    "init-db": "bash init-db.sh",
    "update-data": "cd src/data && Rscript update-db.R",
    "setup": "npm install && bash init-db.sh"
  },
  "dependencies": {
    "@duckdb/duckdb-wasm": "^1.33.1-dev18.0",
    "@observablehq/framework": "^1.13.3",
    "arquero": "^8.0.3",
    "d3-dsv": "^3.0.1",
    "d3-time-format": "^4.1.0",
    "duckdb": "^1.4.3",
    "duckdb-async": "^1.4.2",
    "js-yaml": "^4.1.1"
  },
  "devDependencies": {
    "rimraf": "^5.0.5"
  },
  "engines": {
    "node": ">=18"
  }
}
